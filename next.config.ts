/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media.gettyimages.com",
      "www.shutterstock.com",
      "holdenparksch.b-cdn.net",
    ],
  },
};

module.exports = nextConfig;
